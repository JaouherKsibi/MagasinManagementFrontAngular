import { ListeProduitPipe } from './liste-produit.pipe';

describe('ListeProduitPipe', () => {
  it('create an instance', () => {
    const pipe = new ListeProduitPipe();
    expect(pipe).toBeTruthy();
  });
});
